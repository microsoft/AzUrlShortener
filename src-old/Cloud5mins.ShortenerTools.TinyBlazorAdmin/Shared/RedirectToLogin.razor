@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"/.auth/login/aad?post_login_redirect_uri={Uri.EscapeDataString(Navigation.Uri)}");
    }
}
